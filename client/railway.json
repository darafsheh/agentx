{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
      "nixpacksPlan": {
        "phases": {
          "install": {
            "cmds": ["cd /client", "npm install -g corepack", "corepack enable", "corepack prepare pnpm@9.1.0 --activate", "pnpm install --no-frozen-lockfile"]
          },
          "build":{
            "dependsOn": ["install"],
            "cmds": ["cd /client", "pnpm build"]
          }
        }
      }
    },
    "deploy": {
        "startCommand": "cd /client && pnpm start:client"
    }
  }